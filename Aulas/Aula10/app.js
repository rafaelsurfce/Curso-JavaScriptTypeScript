/*
    Math = Objeto matemática

    math.floor(num) = arredonda para baixa
    math.ceil(num) = arredonda para cima
    math.round = arredonda para o mais próximo
    math.max = retorna o maior número
    math.min= retorna o maior número
    math.random = gera um número aleatório de 0 a 1
*/


let num = 9.4465;

console.log(Math.floor(num));
console.log(Math.ceil(num));
console.log(Math.round(num));
console.log(Math.max(1, 4, 6, 10, 500, 5,));
console.log(Math.min(1, 4, 6, 10, 500, 5,));
console.log(Math.random() * (10 - 5) + 5); //n[umero aleátorio entre 5 e 10 