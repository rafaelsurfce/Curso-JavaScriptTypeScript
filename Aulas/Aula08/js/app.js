/* String = Conjunto de caracteres
     0123456789  = indice       
    "caracteres"
    FUNÇÕES COM STRINGS
    string.charAt(num) = mostra o caractere no indice
    string.concat() = concatena strings
    string.indexOf() = pesquisa dentro de uma string
    string.replace() = substitui uma palavra  da string por outra
    string.length = tamanho da string
    string.slice() = imprime somente o inicio e fim expresso na função da string
    string.toUpperCase() = transforma toda a string em maiuscula
    string.toLowerCase() = transforma todas em minúsculas 
*/
