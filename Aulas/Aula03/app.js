// TIPAGEM

const nome = 'Rafael'; //STRING 
const idade = 25;       //NUMBER
const peso = 71.15;     //NUMBER COM PONTO FLUTUANTE
let sobrenome;          // undefined -> não aponta para lugar nenhum da memória
let nulo = null;        // nulo -> não aponta para lugar nenhum da memória
const booleano =  true || false; // boolean aceita TRUE ou FALSE (LÓGICO)


console.log(typeof(nome), nome);

